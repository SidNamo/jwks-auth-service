# ─────────────────────────────────────────────────────────
# 공통
# ─────────────────────────────────────────────────────────
APP_NAME=auth-service
ENVIRONMENT=loc
LOGGING_LEVEL=INFO
TIMEZONE=UTC

ACCESS_TOKEN_EXPIRE_MINUTES=1
REFRESH_TOKEN_EXPIRE_DAYS=7

# sync alembic 후 db 확인
CLIENT_ID=ci
CLIENT_SECRET=cs

# 어떤 DB 서비스를 기본으로 사용할지 선택합니다.
# config.Settings.database_service 필드에 매핑됩니다.
# 예: main / replica / audit 등
DATABASE_SERVICE=main

# ─────────────────────────────────────────────────────────
# 기본(main) DB 접속 정보
# ─────────────────────────────────────────────────────────
# 도커 컴포즈에서 컨테이너 간 통신이면 서비스명(예: mysql)을 권장합니다.
# 호스트 머신의 MySQL에 붙을 때만 host.docker.internal 사용을 권장합니다.
# DB_MAIN_HOST=mysqld           # docker-compose 서비스명 예시
DB_MAIN_HOST=host.docker.internal
DB_MAIN_PORT=3306
DB_MAIN_USER=user
DB_MAIN_PASSWORD=pw
DB_MAIN_NAME=auth_service

# ─────────────────────────────────────────────────────────
# (선택) replica DB 접속 정보
# ─────────────────────────────────────────────────────────
# 아래 주석 해제 후 값 설정하면 get_db("replica")로 사용할 수 있습니다.
# DB_REPLICA_HOST=host.docker.internal
# DB_REPLICA_PORT=3306
# DB_REPLICA_USER=replica_user
# DB_REPLICA_PASSWORD=replica_password
# DB_REPLICA_NAME=auth_service_replica

# ─────────────────────────────────────────────────────────
# (선택) audit DB 접속 정보
# ─────────────────────────────────────────────────────────
# DB_AUDIT_HOST=host.docker.internal
# DB_AUDIT_PORT=3306
# DB_AUDIT_USER=audit_user
# DB_AUDIT_PASSWORD=audit_password
# DB_AUDIT_NAME=auth_service_audit
